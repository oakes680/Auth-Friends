import React, { createContext } from "react";

export const LoginContext = createContext();



export const LoginContextProvider = props => {
  // ** handle submit

  // ** handle changes

  // ** axios post
  return <LoginContext.Provider ></LoginContext.Provider>;
};
